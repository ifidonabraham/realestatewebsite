{
  "name": "realestatewebsite",
  "version": "1.0.0",
  "main": "email-server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "vercel-build": "node ./scripts/generate-config.js",
    "dev": "node ./scripts/generate-config.js && json-server --watch db.json --port 3000",
    "json-server": "json-server --watch db.json --port 3000"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "body-parser": "^2.2.0",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "nodemailer": "^7.0.5"
  },
  "devDependencies": {
    "json-server": "^0.17.4"
  }
}
