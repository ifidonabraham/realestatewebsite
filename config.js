// Edit the URLs below to your deployed endpoints (Vercel frontend uses these values)
window.APP_CONFIG = {
  API_BASE: 'http://localhost:3000',        // json-server / API (change to your deployed API)
  SOCKET_URL: 'http://localhost:5000',      // chat-server (change to your deployed chat server)
  UPLOAD_URL: 'http://localhost:5000/upload'// file upload endpoint
};